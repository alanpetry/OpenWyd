#pragma once

// The project owns Resource.h; this is only a lowercase include alias for the
// original sources when they are compiled on a case-sensitive host.
#include "../../../../../Projects/TMProject/Resource.h"
